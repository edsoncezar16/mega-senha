{
  "buildCommand": "npm install --prefix client && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "installCommand": "echo skip",
  "ignoreCommand": "git diff --name-only HEAD~1 HEAD | grep -qE '^(client|shared)/' || exit 1"
}
